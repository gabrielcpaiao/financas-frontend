// Paleta definida na etapa de design (teal/verde/vermelho/âmbar) —
// mapeia direto pros componentes prontos do Nuxt UI via prop color="...".
export default defineAppConfig({
  ui: {
    colors: ['primary', 'secondary', 'success', 'warning', 'error', 'neutral']
  }
})
